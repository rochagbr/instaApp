import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

export default function Main() {
  return (
    <SafeAreaView>
      <Text>Helloo</Text>
    </SafeAreaView>
  );
}
